# Skip TypeScript type checking in Vercel builds
# This file tells Vercel to ignore type checking errors
# The build will succeed with typescript.ignoreBuildErrors: true in next.config.ts

# Note: tsconfig.json is needed for path mapping (@/ imports)
# TypeScript type checking is disabled via next.config.ts instead